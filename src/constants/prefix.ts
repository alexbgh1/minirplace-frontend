/**
 * This file contains the prefix for the frontend, should be the same as the one in the vite.config.ts
 * The value is used on deployment to determine the path of the frontend
 */
const FRONTEND_PREFIX = "minireplace-frontend";

export { FRONTEND_PREFIX };
